# railway.yaml
version: 1
environments:
  production:
    build:
      commands:
        - pip install -r requirements.txt
    run:
      command: bokeh serve --allow-websocket-origin=* --port=$PORT mainapp.py
